#pragma once

#include <iostream>
#include <string>
#include <iomanip>
#include <ctime>

using namespace std;

extern void ShowArr(int arr[], int arrSize);
extern void DrawTable(string title, int* values);